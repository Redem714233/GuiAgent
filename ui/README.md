# UI

前端基于 React + Vite，用于输入自然语言任务、查看实时执行过程和下载输出文件。

## 启动

```powershell
npm install
npm run dev
```

默认开发地址：
- `http://127.0.0.1:5173`

## 后端地址

当前代码中默认后端地址为：
- `http://127.0.0.1:8000`

请先启动后端服务，否则前端无法执行任务。

## 打包

```powershell
npm run build
```

